[
	{
		"type": "function",
		"function": {
			"name": "run",
			"description": "complete a task with an automation flow",
			"parameters": {
				"type": "object",
				"properties": {
					"task": {
						"type": "string",
						"description": "description of the task, and the essential flow to complete it"
					}
				},
				"required": ["task"]
			}
		}
	},
	{
		"type": "retrieval"
	}
]
